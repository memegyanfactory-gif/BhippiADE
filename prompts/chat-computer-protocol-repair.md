<!-- version: 1 -->
`<{{tag}}>` belongs to another Bhippi tool protocol, which does not exist in this turn.
That request was not executed and the screen is unchanged.

For work in the visible application, return exactly one Computer Use action and inspect
the resulting screenshot. For project tools, asset registration or a Blender Python build,
hand the task back with `<engine_request>{"reason":"describe the project work needed"}</engine_request>`.
The project phase can use those tools and return their actual results. Do not claim a
script ran or an asset was created merely because you wrote its tool tag.

{{example}}
