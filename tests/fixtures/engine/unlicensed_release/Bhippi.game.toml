# Unlicensed Release — Bhippi game project (format v1)
# Generated by Bhippi. Edit freely; sections are validated on load.

[game]
id = "01M1ES28XS18BGP6D0Y6SHXNS2"
name = "Unlicensed Release"
version = "0.1.0"
default_scene = "assets/scenes/main.bscn.json"
engine_track = "rust"
hud_scene = "assets/ui/hud_main.hud.json"
levels = ["assets/scenes/level_01.bscn.json"]

[render]
pipeline = "3d"
msaa = 4

[physics]
backend = "avian"
gravity = [0.0, -9.81, 0.0]

[targets.windows]
enabled = true
[targets.android]
enabled = false
package = "com.example.mygame"
min_sdk = 24
[targets.ios]
enabled = false
bundle_id = "com.example.mygame"
[targets.web]
enabled = true
canvas_fit = "window"

# What the agent may do to this project (ENG-190). Each key is allow / ask / deny;
# anything not listed takes its default: edit_scene, create_content, write_script
# and run_play are allowed, delete, import and build are asked for first.
[agent]
