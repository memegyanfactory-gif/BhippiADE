# Bhippi — Progress

**This file is the single source of truth for project state.** Every agent reads it at
session start and updates it before finishing. If it disagrees with the code, the code wins
and your first act is to fix this file.

**Last updated:** 2026-09-02 · **Current sprint:** S0 (closing) · **Build status:** BHP-001…008 complete; BHP-009 verification is active on GitHub; S1 early slice started; Engine Phase 7 complete; Phase 8 reference-GPU and host-launch evidence remains; `/gamedebug` Phase 9 first slice landed; quality/sandbox Phases 10–12 remain planned; Phase 13 minimal editor reset has started (ADR-0034 and first shell slice); expansion Phases 14–24 remain planned; Project/World Brain persistence and query slices done; Token Engine Phase A complete

States: `todo` · `in-progress` · `blocked` · `done`
A `blocked` row must name the blocker.


## Sprint status

| Sprint | Scope | Tickets | State | Exit criterion |
|---|---|---|---|---|
| S0 | Foundations | BHP-001…009 | in-progress | app opens, DB migrates, replay dumps, CI green ×3 platforms |
| S1 | Providers | BHP-010…024 | todo | prompt runs on Ollama + one CLI, visible fallback |
| S2 | Harvest | BHP-030…048 | todo | extraction F1 ≥ 0.92; robots honoured under test |
| S3 | Research I | BHP-060…078 | todo | X2 + X6 complete maps on golden topics in budget |
| S4 | Research II + map UI | BHP-079…092 | todo | 500 nodes ≥ 55 fps; fact gate blocks a seeded claim |
| S5 | Memory | BHP-100…118 | todo | ≥ 30 % fewer redundant fetches on paired run |
| S6 | Vision | BHP-130…152 | todo | every image licensed; crops geometry-verified |
| S7 | Writer + SEO | BHP-160…202 | todo | zero hard lint failures over 30 posts |
| S8 | Publish + theme | BHP-203…216 | todo | Lighthouse SEO ≥ 95 / Perf ≥ 90; rollback works |
| S9 | Ticker + automation | BHP-220…272 | todo | one wire story ⇒ one session; 24 h soak clean |
| S10 | Skills | BHP-280…308 | todo | red team green; authored skill beats baseline |
| S11 | Hardening + beta | BHP-320…386 | todo | all §24 budgets met; zero P0/P1 |
| Engine track | Game-engine workbench | ENG-001…399 | in-progress (Phase 7 complete; Phase 8 evidence remains; Phase 9 `/gamedebug` first slice landed; quality/sandbox Phases 10–12 planned; Phase 13 shell simplification started; Phases 14–24 planned) | Phase 13 real-engine screenshots/task timings/bottom drawer; capability-registry/GameSpec goldens; `/gamedebug release` quality + sandbox evidence; subsystem budgets and host export proof |


## Ticket board — S0 (expand the next sprint's rows when it starts)

| Ticket | Sprint | Crate | Work | State | Updated | Invariants | Evidence |
|---|---|---|---|---|---|---|---|
| BHP-001 | S0 | workspace | Cargo workspace, 14 crates, MSRV, lint config | done | 2026-08-26 | INV-036, INV-060 | `cargo +stable fmt --check`; clippy `-D warnings`; workspace tests; workspace build |
| BHP-002 | S0 | types | ids, `Tier::budget()`, Stage, TaskClass, Event, Error | done | 2026-08-26 | INV-040 | budget snapshot, ID round-trip, event tag, actionable-error unit tests |
| BHP-003 | S0 | db | migrations 0001–0003, repositories, sqlx queries | done | 2026-08-26 | INV-020, INV-042 | fresh migrate + doctor; atomic stage rollback; SQL boundary; workspace gates |
| BHP-004 | S0 | core | config loader + keychain wrapper | done | 2026-08-26 | INV-004, INV-037, INV-039 | config round-trip; safety-setting rejection; secret scan; workspace gates |
| BHP-005 | S0 | core | event bus + 20/s coalescer | done | 2026-08-26 | INV-021 | lossless burst batch; combined rate cap; lag resync; absent-subscriber test |
| BHP-006 | S0 | core | tracing, rolling JSON logs, secret scrubbing | done | 2026-08-26 | INV-037 | exact + prefix redaction; seeded-key rolling JSON test; workspace gates |
| BHP-007 | S0 | core | **session replay dumper** | done | 2026-08-26 | INV-037 | resume-does-not-duplicate; manifest rewrite; redaction; 3 tests green |
| BHP-008 | S0 | app/ui | Tauri shell, screens, chrome, tokens, single-instance | done | 2026-08-26 | INV-032, INV-034 | desktop launches on Windows (smoke-tested); specta bindings generated (`ui/src/lib/ipc.ts`); chat screen per ADR-0006; Research/Automation/Library/Settings render all four states |
| BHP-009 | S0 | ci | fmt → clippy → architecture test → build matrix | in-progress | 2026-08-26 | INV-060 | `.github/workflows/ci.yml` authored (3-OS matrix + ui build + ipc-freshness job); **blocker: no git remote yet — push to verify green** |
| BHP-010 | S1 | providers | `Provider` trait + `Capabilities` + streaming `Delta` | done | 2026-08-26 | INV-001 | trait + NDJSON parser fixture tests; clippy `-D warnings` clean |
| BHP-018 | S1 | providers | Ollama native adapter (first slice) | in-progress | 2026-08-26 | — | `/api/chat` NDJSON streaming + probe implemented; capability probing/tok-s bench outstanding |
| — | S1 | providers | Demo offline provider (ADR-0006) | done | 2026-08-26 | — | deterministic scripted stream, always badged `demo`; unit-tested |
| BHP-016 | S1 | providers | **CLI adapter made to actually answer**: Windows launcher fix, JSON transcripts, live model catalogues | done | 2026-08-26 | INV-003, INV-032 | 38 provider tests green incl. a multi-line-argument launcher regression pin and Codex/OpenCode/Claude transcript fixtures; verified live end to end — Claude Code, Codex and OpenCode each answered a two-line prompt exactly, with real token counts |
| — | S1 | app/ui | Conversation delete, remembered provider, usage panel rebuild (ADR-0011) | done | 2026-08-26 | INV-032, INV-034 | `delete_conversation` engine test; `last_provider` config round-trip; 4 new usage tests (window→chart span, shares sum to 1, colour follows provider, range label); palette validated against `--surface`; panel rendered and eyeballed |
| — | S1 | providers/app/ui | **Live CLI streaming, typed faults, agent phases** (ADR-0016): `output()` → spawn + line-by-line reader; Claude on `stream-json --include-partial-messages --strict-mcp-config`; 13-kind fault classifier with per-fault remedy buttons; in-band failure detection on clean exits; pre-send context guard; live 5h/weekly limit banner; 28-state `AgentPhase`; vendor tool steps into the activity dock; update-check before auto-install; motion system + 3 new themes | done | 2026-08-27 | INV-003, INV-032, INV-034 | 40 provider + 33 app tests green incl. a **timing** regression pin (`tests/streaming.rs`) asserting the first delta lands before the child exits, and an in-band-failure pin; live `claude` run measured `ttft_ms: 1266` vs. a previously full-turn wait; `cargo fmt --check` + clippy `-D warnings` clean; `tsc --noEmit` + `vite build` clean; desktop binary launched and screenshotted with the rebuilt composer bar |
| — | S1 | app | **`/debug` rebuilt as a real debugger** (docs/11): recursive walker with ignore rules and stated budgets; 20 hardcoded rules across correctness/security/reliability/hygiene, each with a positive *and* negative test; all stacks run (was an exclusive if/else); tsconfig discovered anywhere; clippy over check; per-tool timeouts. Plus **Bhippi Design System** (docs/DESIGN-SYSTEM.md) and its per-turn switch in the effort drop-up | done | 2026-08-27 | INV-032, INV-034, INV-036 | 39 suites green incl. a seeded-project integration test asserting six defects that compile perfectly are all found, and that skipped directories never leak into a report; `cargo fmt --check` + clippy `-D warnings` clean; `tsc --noEmit` + `vite build` clean |
| — | S1 | providers/app/ui | **Local servers detected, never started** (ADR-0017): removed the `--version` probe that launched Bionic's GUI on every sweep; `usable()` replaces `installed` so an idle server is unselectable; default order is running-local → ready-remote → demo; model ejected on switch-away (Ollama/LM Studio real, others reported honestly); port probes made concurrent | done | 2026-08-27 | INV-003, INV-032 | 39 suites green incl. pins that an unlistening server is never healthy and never default; detection tests 22.09s → 2.05s; **verified live**: Bionic closed, app run 40s, zero `Bionic.exe` spawned, log shows `default: claude` |
| BHP-024 | S1 | core/providers/app/ui | **Usage metering + amber accent** (ADR-0009): persistent per-provider token/cost ledger, status-bar gauge with drop-up, Settings › Usage | done | 2026-08-26 | INV-032, INV-034, INV-036, INV-042 | 15 new unit tests green (ledger accumulate/cap/range/reload, summary window scaling, uncapped ring, chart zero-fill, cap round-trip through `config.toml`); `cargo fmt --check` + clippy `-D warnings` clean; `tsc --noEmit` + `vite build` clean; desktop app screenshotted showing the ring at 0 % (empty), 96 % (red, near-full), the drop-up, and the Usage panel |
| — | S1 | core/providers/app/ui | **Project-first ADE shell** (ADR-0012/0013): persistent start rail, native folder selection, project-partitioned conversations, canonical CLI workspace, editor launchers, Git init, centred-to-docked composer, Ultra particles | done | 2026-08-26 | INV-003, INV-032, INV-034, INV-035, INV-036 | 18 app + 39 provider + 6 config + 2 architecture tests green; cross-project list/read/delete/send/regenerate blocked; IPC regenerated; UI production build, fmt, clippy `-D warnings`, embedded desktop build clean |
| — | S1 | app/providers | **Computer Use self-engagement + vision-provider handoff** (ADR-0018): widened intent gate (verb+desktop-object heuristic, dev-discussion veto kept); auto-handoff to the first enabled/authorised vision CLI when the picker's choice cannot see the desktop (claude→codex→grok, never a silent swap — thinking line + session note + transcript note); desktop-loop prompt v3 with a 1:1 pixel→coordinate contract; live loop proofs in `tests/computer_loop_live.rs` (deterministic synthetic-vision agent completes capture→observation→tag→validation→execution→pointer-landing against the real desktop; real-CLI variant spawns the vendor executable, skipping only on an exhausted account) | done | 2026-08-27 | INV-003, INV-032, INV-036, R5 (prompt file), no-`unwrap` | 62+71+… workspace suites green; fmt + clippy `-D warnings` clean; live synthetic loop passed twice (cursor landed at screen centre, verified, restored); real claude/codex/grok CLIs each spawned and streamed through our argv — all three accounts are currently at their vendor usage limits (billing, not code); live check done **2026-08-27**: claude returned `<computer_action>{action:mouse_move,x:960,y:540}</computer_action>`; the relaxed-JSON fallback parsed it and the pointer landed and was verified at the screen centre |
| — | S1 | app/providers/ui | **Natural + forced Computer Use on-ramp**: recognises literal `use computer` / `use pc` and `access my computer` / `access my pc`; `/computer <task>` is an explicit per-turn override that bypasses the conservative discussion veto; the slash palette inserts the command for the user to finish rather than sending an empty task; "Computer + Browser included" remains a real persisted toggle | done | 2026-08-27 | INV-003, INV-032, INV-036 | focused intent tests cover the exact failed wording, forced-command boundary, and veto bypass; production UI build, fmt, and clippy `-D warnings` clean; live synthetic desktop loop captured the real display, moved the real pointer to the screen centre, verified the landing, and restored it; prior live model E2E remains green |
| — | S1 | providers/app/ui | **Provider update recovery + Computer Use emergency stop**: Update now shows progress, verifies the CLI, and retries; Settings › Providers has a working Update button on installed CLIs; overlay listens on Tauri internals so the corner aura and pointer track; Esc/Esc is armed even if the overlay window fails; Codex `-m` sits inside `exec` before the prompt | done | 2026-08-30 | INV-003, INV-032, INV-034, INV-052 | provider CLI argv tests (Codex model-before-prompt, Grok `-p` prompt, Computer Use flags) green; overlay unit tests green; UI `tsc --noEmit` + production build clean; clippy `-D warnings` on the changed crates clean; desktop binary rebuilt and relaunched |
| — | S1 | app/ui | **Glass amber shell**: default appearance is Classic Amber + glass + Ember Flame; warm radial wash in the main pane; gold edge sheen on composer/sidebar/popovers; filled amber send | done | 2026-08-30 | INV-034 | UI `tsc --noEmit` + production build clean; desktop rebuilt and screenshotted against the amber glass reference |
| — | S1 | app/ui | **Project-card session rail** (W4 §4.2): `list_workspace_sessions` engine method + command return every conversation across all projects as `WorkspaceSession` (kind, provider id via registry label→id match, `provider_label`, live `session_status` from the latest turn — running/paused/idle/failed); sidebar rewritten as one smoked-glass card per project with provider/CLI session chips, live status dots, hover-revealed two-click delete, "N active" pill, and overflow "Show all N"; empty projects keep a "+" to start their first session; CLI sessions merge into the rail client-side from localStorage | done | 2026-08-30 | INV-032, INV-034, INV-036, R3 | 4 new backend tests green (`session_status` mapping incl. `AwaitingPermission → Paused`, empty-conversation Idle, latest-turn derivation, sort by `updated_at` desc); `cargo fmt --check` + clippy `-D warnings` clean; bindings regenerated; UI `tsc --noEmit` + production build clean |
| — | S1 | app/ui | **Multi-session project workspace** (ADR-0023): persistent Single/Multi switch; every active-project chat and CLI visible together; Organize popover with auto-fit and three layout strategies; pointer/keyboard manual resizing bounded to a 300 px control floor and 85% workspace ceiling; turn-id event isolation across mounted chats; draggable project ordering plus stable pin group; explicit loading/empty/error/populated states | done | 2026-08-30 | INV-032, INV-034, R3 | UI `tsc --noEmit` + production build clean; no IPC, dependency, database, or project-boundary change |
| — | S1 | app/ui | **Magnetic Multi gesture + minimal project rail** (ADR-0023): dropping one Single-mode session tab onto another opens Multi view with a labelled merge target; overflowing tab and panel rails pan horizontally with proximity snapping; sidebar tightened to the 240 px shell contract with flat project rows, restrained active treatment, compact session chips, and hover/focus-only secondary actions | done | 2026-08-31 | INV-034, R3 | UI `tsc --noEmit` + production build clean; existing multi-workspace loading/empty/error/populated states preserved; visual Preview unavailable because Agent Mode remained off |
| — | S1 | app/ui | **Smart fit preview/runtime parity** (ADR-0023): corrected the four-window runtime from a 2×2 grid to the organizer preview's full-height first ordered primary plus three evenly stacked secondary windows; three-window Smart fit follows the same rule; focus and typing do not move windows; narrow workspaces still stack vertically | done | 2026-08-31 | INV-034, R3 | UI `tsc --noEmit` + production build clean; supplied organizer screenshot used as the exact layout reference; visual Preview unavailable because Agent Mode remained off |
| — | S1 | app/ui | **Sidebar cleanup + project-scope chat fix**: removed the top project badge/dropdown (identity lives in each card head); "New session" → full-width **New project** menu (Open a folder / Create a project / Clone from Git — all browse with the Windows native picker, `dialog:allow-open` already granted); Chat/CLI sessions start only from a project card's `+`; Chat screen keyed by `${screen}:${activeProject.path}` so switching projects swaps the session scope; `openExternalTerminal` wrapper dropped from App (CliView already calls the API directly) | done | 2026-08-30 | INV-032, INV-034 | UI `tsc --noEmit` + production build clean; clippy `--workspace -D warnings` clean; desktop rebuilt + relaunched; UIA-verified project-switch chat scope, per-project "New chat in …", dropdown removal, and the New-project menu's three options |
| — | S1 | core/app/ui | **Token Engine Phase A — measurement first** (plan §45): per-turn context telemetry (`bhippi_core::ContextSampleStore`, 11-category ledger, `~/.bhippi/context.json`, local-only INV-039, 2 000-sample cap); IPC `get_context_summary`/`clear_context_samples` + regenerated bindings; deterministic `capture-baseline` bin → `docs/token-engine/baseline.md`+`.json` (A2/A6, 5 representative tasks on the offline demo provider); tool-schema = 0 measured (A3); handoff note = 81 est. tokens, plus a fix where the prior-provider *label*-vs-*id* comparison tagged every turn incl. firsts as a handoff (A4); workspace/rules rows recorded per turn (A5) | done | 2026-08-31 | INV-032, INV-036, INV-039, R3 | 6 core + 3 app context-summary + 2 baseline tests green; baseline report mean est. input 4 251 tokens, follow-up history grows 1→3 msgs; `cargo fmt --check` clean; clippy clean; workspace `cargo test` green (85 app / 15 core) |
| — | S1 | app/ui | **Windows cursor safety & overlay click-capture fix**: eliminated OS-wide system cursor blanking (`SetSystemCursor`), auto-restore cursor on app startup (`restore_system_cursor`), disabled desktop-spanning webview that intercepted Windows mouse clicks, eliminated runaway PowerShell polling process, set `pointer-events: none !important` on overlay HTML/CSS | done | 2026-08-31 | INV-032, INV-034, INV-036 | 87 app tests green; `cargo fmt --check` + `clippy -D warnings` clean; `npm run build` clean |
| — | S1 | app/ui | **Project panel deduplication & conversation preservation fix**: normalized project path comparison in `workspace.rs` using `paths_match`, auto-deduplicated `config.toml` project records, prevented redundant conversation creation on project card click, preserved active conversation when switching projects or creating sessions | done | 2026-08-31 | INV-032, INV-034, INV-036 | 87 app tests green; `cargo fmt --check` + `clippy -D warnings` clean; `npm run build` clean |
| — | S1 | app/ui | **Multi-mode options popovers & concurrent independent chat sessions fix**: resolved `.multi-session-workspace .composer-bar` `overflow-x: auto` clipping that blocked Provider/Model/Effort/Options popovers; added `.session-panel:focus-within` stacking elevation; keyed `<Chat key={activeConversationId}>` in Single Mode to isolate conversation lifecycle and state; added per-conversation draft retention and per-conversation provider selection so concurrent turns run independently across chats | done | 2026-08-31 | INV-032, INV-034, INV-036 | 111 backend tests green; `cargo clippy --workspace -- -D warnings` clean; `npm run build` clean |
| ENG-200/201/206/207/208 | Engine Phase 9 | engine/app/ui | **`/gamedebug` first slice**: ADR-0032; fixed nine-stage engine graph; manifest/scene/HUD/input/material/shader/asset/licence/script static checks; authored before/after hashes; stable findings with evidence/reproduction/repair; offline slash parsing and composer discovery; immutable ULID JSON/Markdown reports plus latest pointer. Full/release runtime stages are explicitly unsupported, never false-pass. | in-progress | 2026-09-02 | INV-073, INV-074, INV-081, INV-082 | 4 engine + 2 app focused tests; full `cargo test --workspace`; workspace/all-target clippy `-D warnings`; fmt/diff checks; TypeScript + Vite production build green |
| ENG-240…399 | Engine Phases 13–24 | docs/engine/ui/runtime | **Unreal-class expansion audit incorporated.** Added a conservative capability matrix, seven-dimensional truth vocabulary, minimal fixed editor shell, density/usability acceptance, central capability registry, GameSpec/composition planner and dependency-ordered subsystem phases. Phase 13 has since started in the row below; Phases 14–24 remain unimplemented. | in-progress | 2026-09-01 | existing invariants; new ADRs required per phase | Complete 160-ticket continuity audit; no later subsystem promoted by documentation |
| ENG-241/243; ENG-242/245/246/250/251/253 | Engine Phase 13 | ui/docs | **Minimal Engine shell first slice:** ADR-0034 supersedes docking-first; quiet main toolbar, viewport context strip, Scene/HUD mode rail, focused AI and More menus, responsive degradation, all existing handlers/shortcuts preserved. Bottom-drawer consolidation and real-engine visual/task evidence remain. | in-progress | 2026-09-02 | INV-034, INV-073 | UI build green; 62/62 UI tests including 3 shell guards; browser preview booted, but project-backed Engine capture requires the Tauri host |
| — | S1 | app/ui | **Bottom-docked composer position parity & empty chat decluttering**: eliminated pristine `margin-bottom: 6vh` and `justify-content: center` so composer always docks in the exact same bottom position whether brand new or actively chatting; removed noisy middle text (project session eyebrow, title, description, and suggestions chip row) on new chat creation leaving a clean, distraction-free thread canvas | done | 2026-09-01 | INV-032, INV-034, INV-036 | `cargo fmt --check` + `clippy -D warnings` clean; `npm run build` clean; `cargo build` clean |


## Open decisions (ADR needed)

| # | Question | Needed by | State |
|---|---|---|---|
| A1 | Topical classifier in `harvest` or `core`? | S2 | open |
| A2 | Barnes-Hut layout in `core` or its own crate? | S4 | open |
| A3 | Tantivy commit cadence — per expansion or per session? | S3 | open |
| A4 | `sqlite-vec` vs. brute force below 50k vectors | S5 | open |
| A5 | React SSG renderer: shared loader crate or JS build step? | S8 | open |

Answer with an ADR in `docs/adr/`, then mark the row `closed → ADR-nnnn`.


## Known risks being carried

| Risk | Current mitigation | Owner |


## Open decisions (ADR needed)

| # | Question | Needed by | State |
|---|---|---|---|
| A1 | Topical classifier in `harvest` or `core`? | S2 | open |
| A2 | Barnes-Hut layout in `core` or its own crate? | S4 | open |
| A3 | Tantivy commit cadence — per expansion or per session? | S3 | open |
| A4 | `sqlite-vec` vs. brute force below 50k vectors | S5 | open |
| A5 | React SSG renderer: shared loader crate or JS build step? | S8 | open |

Answer with an ADR in `docs/adr/`, then mark the row `closed → ADR-nnnn`.


## Known risks being carried

| Risk | Current mitigation | Owner |
|---|---|---|
| Toolchain moved to stable / effective MSRV 1.85 (ADR-0007) | edition-2024 deps made 1.79 untenable; CI installs stable; our-code discipline unchanged (`forbid(unsafe)`, deny unwrap/expect) | Tech Lead |
| Local model too weak for Writer | local quality mode: tighter blueprints, shorter sections, harder lint; document a ≥ 14B floor | Agents |
| Drift at depth | cosine guard + charter gap scoring + human raters each sprint | Agents |
| Auto-publishing something wrong | gates land before automation (S4/S6/S8 before S9) | Core |
| Crawl blocking | conservative limits, feeds first, per-host breakers | Core |
| Scope creep to a general research tool | domain lock is a HARD REQ; new categories need an ADR | Tech Lead |


## Session log

| Date | Agent | Did | Learned | Next agent should |
|---|---|---|---|---|
| 2026-09-02 | codex | Verified and prepared publication of the `/gamedebug` Phase 9 first slice and Phase 13 minimal-shell first slice: fixed nine-stage diagnostics, immutable reports, slash-command route, quiet editor toolbar, mode rail, contextual viewport strip, collapsed-by-default content drawer, responsive degradation and regression guards. | A browser preview cannot produce honest project-backed Engine evidence because Tauri project IPC is absent; the slice therefore stays explicitly in progress even though all headless, Rust, architecture, accessibility and UI gates pass. | Publish the scoped commit, then continue Phase 13 with ENG-244 Play options and ENG-247 shared bottom drawer without marking the full phase complete. |
| 2026-09-01 | codex | Prepared the first public GitHub publication: professional root README, official AGPL licence, contribution/security guides, PR checklist, line-ending policy, comprehensive ignore rules, secret scan, and a fresh-checkout CI fix that builds `ui/dist` before compiling Tauri. Initialized `main` for `memegyanfactory-gif/bhippiADE`. | Formatting, strict clippy, all 59 UI tests, accessibility checks and the production UI build pass. Full Rust tests cannot link in this shell because Visual C++ Build Tools (`link.exe`) are not installed; remote CI is the authoritative three-platform gate. | Push the exact reviewed commit, verify GitHub Actions on Windows/macOS/Linux, then mark BHP-009 done only if the remote checks pass. |
| 2026-09-01 | codex | **Engine closure wave.** Closed ENG-115, 126, 136, 142, 144, 147, 152, 165 and 166: bounded AI telemetry queries; deterministic room/corridor authoring; safe HUD reparent/reorder; schema-owned multi-edit/reset; full viewport cameras/view modes/screen percentage/maximise; project-scoped file/asset quick-open; the automated accessibility matrix; runtime-truth collider/bounds visuals; and selected-camera PiP. Advanced ENG-107 with incremental identity-preserving scene patches and ENG-149 with a shared typed console plus exact source navigation. | Cross-surface tickets only close when their shared source of truth is tested: schema defaults live in Rust, collider debug uses Play’s resolver, panel/model console filtering uses one store, and editor projections preserve untouched render-resource identity. | Remaining implementation is explicitly retained in docs/13: provider-native tool calls, importer/reimport, docking/folders/thumbnails/advanced gestures, play-mode window semantics/keymap, console restart persistence, browser-GPU fps and launched-host release evidence. |
| 2026-09-01 | codex | Recorded the owner’s final publication target, `https://github.com/memegyanfactory-gif/bhippiADE`, in docs/13 as a post-verification gate with fetch/divergence, secret/artefact review, exact-commit test, non-force-push and remote-CI verification requirements. | The owner wants one final Git update when the entire engine plan is implemented—not a planning-only push labelled complete. | Complete and verify every remaining engine ticket first; only then connect/confirm the exact remote, publish the verified commit without overwriting remote work, and record its SHA + CI result. |
| 2026-09-01 | codex | Completed and implementation-reconciled `docs/13-ENGINE-AI-CONTROL-AND-UNREAL-UX-PLAN.md`: corrected false-complete checkboxes, documented the interrupted Phase 7 observation/playtest work as in progress, added dependency-ordered closure contracts for every remaining ticket, retired stale INV-078 work, and specified offline/GPU/host E2E evidence. | “Plan complete” and “implementation complete” must be separate states. The tree already contains a bounded screenshot/playtest/context bridge from an interrupted task, but without cross-boundary fixtures and the warehouse repair golden it cannot honestly close Phase 7. | Finish ENG-186 capture contract and tests first, then ENG-187 runner validation, ENG-188 repair fixtures, ENG-191 baseline measurement, and finally the ENG-185 warehouse golden; use the closure table in docs/13 as the execution order. |
| 2026-09-01 | claude | **Phase 6 closed, Phase 7's safety half, and a new chat-surface plan (docs/14).** ADR-0030 splits gameplay scripting: `bhippi-engine::script` compiles a documented Rhai subset to bytecode with per-instruction line spans, `ui/src/engine/scriptVm.ts` executes it with a step budget and a call-depth cap, and neither `eval` nor a parser exists in the pane (new INV-082, enforced by `tests/architecture.rs`). The AABB solver is gone: real cuboid/sphere/capsule/heightfield colliders against oriented boxes, so `max_slope` and `step_height` mean something. `create_script` compiles before it writes. New per-project `[agent]` capability policy (allow/ask/deny) gated at the single batch choke point; scene leases plus an optimistic revision check so an agent is told "changed under you" instead of overwriting; "Undo AI change" over a whole journalled batch. Then, for the chat: a written plan (`docs/14-CHAT-SURFACE-PLAN.md`) and its first two phases — steps now record command, output, exit code, elapsed and real line counts, and the transcript renders collapsible activity groups, bounded command output, a "Worked for 13m 42s" line, a notice lane, and an "Edited N files +X −Y" card with working Undo and Review. | ENG-176 had been open because Rhai is a Rust crate and ADR-0028 put the runtime in the webview; ticking it either way would have been a lie, so the ADR names the split instead. On the chat: every visual gap from the target was downstream of one finding — a step was five strings, so an activity row could not expand into anything. A cross-language fixture (`pickup.rhai` → `pickup.program.json`) fails in Rust if the compiler and the VM drift, and a prompt-drift test fails if `chat-engine.md` stops listing a host function the compiler accepts. Also fixed: the repo's `cargo clippy --all-targets` gate had been red workspace-wide (`expect_used` firing in every test module). | Phase 7's autonomy half — ENG-186 (`engine_screenshot`), ENG-187 (AI playtest), ENG-185 (loop driver), ENG-188 (repair round), ENG-191 (context budget) — which is one coherent piece of work: the agent must be able to see and drive the running game. Then chat Phase C (composer, sticky header, scroll affordance, accessibility) and CHT-113's expandable hunk view. |
| 2026-09-01 | codex | **ENG-104, 170–175, 177–180, 196 runtime/recovery slice.** Added validated `bhippi-input@1` and scaffolded `assets/input.json`; play consumes manifest gravity and component controller values through an isolated deterministic runtime, supports collisions/sensors, named input, live HUD bindings/actions, persistent Main+HUD level travel, camera possession/eject, Pause/Step/Restart/time scale/Game View and diagnostics. Every edit writes a crash snapshot; a fresh process offers Recover/Discard and Save clears it. | The old simulation hard-coded WASD, speed, gravity and floor height inside one React effect. `M15` also still described the removed Bevy child process despite ADR-0028. Runtime isolation makes INV-081 testable: simulation never mutates the authored document. | Finish ENG-176 with an ADR-approved Rhai strategy and replace the conservative AABB solver with full collider shapes/slope handling; then wire runtime control/screenshot/playtest into the AI autonomy loop. |
| 2026-09-01 | claude | **Phase 4 — Unreal-grade editor UX.** `engine_component_schema` and `engine_list_assets` expose the engine's component registry and asset index over IPC, and the Details panel is now generated entirely from them — accordions by category, a control per `FieldKind`, asset pickers filtered by `AssetKind`, property search, Add/Remove Component from the live registry. The Outliner became a real tree: expand/collapse, drag-to-reparent, Ctrl/Shift multi-select, per-row visibility and lock, type icons, search and filter chips including **AI-made** (which works because ENG-127 stamps provenance). New `EngineCommandPalette` (`Ctrl+Shift+P`, fed the same handlers the toolbar calls) and `EngineOutputLog` (the transaction journal plus local notices, so "what did the agent change?" survives a restart). Change toasts name the actor and offer one-click Undo. Viewport gained a Show-flags menu and a camera-speed slider. | **ENG-146 closed F9, the last structural defect from the original audit:** the viewport added every object flat to the scene group under a comment admitting the hierarchy was "logical, not transform-accumulated", so moving a parent left its children behind — which is why prefabs, rigs and grouped level pieces could not work. Objects are now parented to each other, `Transform` is local as the stable-path addressing (`scene:/Parent/Child`) always implied, and a gizmo drag writes the local transform so a dragged child takes its own children with it. ENG-140 (docking) was deliberately not started: every other Phase 4 ticket makes something possible that was not, while docking makes something movable, and the renderer still cannot draw a material. | Phase 5 (`ENG-160…168`, rendering truth): GLTF loading, PBR material application, lighting parity, sky/IBL, editor visuals, the 55 fps INV-077 gate, and the **ENG-168 decision** — build the Bevy viewport behind the JSON-RPC protocol that already exists, or amend ADR-0020 to make the webview viewport the shipping renderer. That decision has been "next" since 2026-08-29 and now blocks Phase 6's physics. Run `cargo test --workspace` (463 green, 53 suites) + `cargo clippy --workspace` (clean) + `cargo fmt --check` + `tsc --noEmit` + `npm run build` + `cargo build -p bhippi-app --bin bhippi-desktop`. |
| 2026-09-01 | claude | **Phase 3 — the HUD system**, plus a build fix. New `bhippi-engine` modules `hud.rs` (`bhippi-hud@1`: canvas, anchored rects, style, bindings, a closed click-action list, twelve widget kinds with typed field schemas) and `hud_action.rs` (thirteen edit verbs plus `resolve_rect`, the anchor maths the canvas editor and the runtime overlay share). New app module `engine/hud_session.rs`: open HUD documents with a snapshot undo stack, a widget catalog for the Add menu, and widget views whose rects arrive pre-resolved so the webview computes nothing. Nine `hud_*` IPC commands, a `HudChanged` event, and `EngineHudEditor.tsx` — a Scene/HUD tab in the Engine pane with a widget tree, a canvas preview and a Details form generated from the engine's schema, committing a whole form as one undo step. ENG-139: the scaffold now writes `assets/ui/hud_main.hud.json`, the manifest and Main point at it, `compose_play` stopped merging HUD entities into the 3D world, and `engine_play_world` returns the HUD document plus resolved widgets so Play draws the overlay from the *live session*. | The old HUD was entities carrying `UiDocument { layout: "health" }` — a magic string with no fields behind it, which is precisely why a button's text could not be changed: there was nowhere for the text to live. Also fixed here: the **plugins feature another agent left half-finished** was breaking `tsc` and the whole UI build (27 errors). Its Rust side was complete but never registered in `lib.rs`, so `PluginMetadata` never reached `ipc.ts`; `Sidebar.tsx` had a clobbered `useEffect` opener, a duplicate local `PluginMetadata`, missing icon and `api` imports, a `"plugins"` value absent from the `Screen` union and a dead `onOpenPlugins` prop; `Chat.tsx` had its own copy of `PluginMetadata` typed `window?: PluginWindow` where the wire type is `window: PluginWindow \\| null` — the exact drift that hand-copied generated types produce. Registered, wired and unified; the app builds and runs again. | Phase 4 (`ENG-140…152`, Unreal-grade editor UX: docking, Outliner folders, schema-driven Details for *scenes*, Content Browser tiles, viewport toolbar, marquee select, command palette, output log) or Phase 5 (rendering truth — the renderer still does not draw materials or imported meshes, which is the last thing standing between "the AI generated it" and "I can see it"). Run `cargo test --workspace` (452 green, 53 suites) + `cargo clippy --workspace` (clean) + `cargo fmt --check` + `tsc --noEmit` + `npm run build` + `cargo build -p bhippi-app --bin bhippi-desktop`. |
| 2026-09-01 | claude | **Phase 2 — real content generation.** New engine modules: `material.rs` (`bhippi-material@1` + `bhippi-shader@1`, refusing out-of-range values rather than clamping), `prefab.rs` (`bhippi-prefab@1` — capture, instantiate with fresh ULIDs, override-aware propagation that never moves an instance), `procedural.rs` (pinned SplitMix64 + grid/scatter/ring/perimeter/stack, seeded and capped) and `gates.rs` (`check_project` + `check_assets`, typed blocker/warning findings). New app module `engine/content.rs`: `ContentAction` (`create_material`, `create_shader`, `create_prefab`, `set_asset_license`) producing a `FileChange` that carries prior bytes, so the inverse is exact; content steps ride inside the same batch as scene actions and the session keeps a file ledger keyed by transaction id, making "create a material and put it on the floor" one Ctrl+Z. Five new placement verbs make the procedural helpers callable. Every spawn is stamped with `Provenance` (actor, txn, timestamp) in `commit_ops`. The gates are wired where they block: `bhippi-engine-build::prepare` fails on any blocker, and `engine_check_content` exposes the report. `prompts/chat-engine.md` v6. | Three real defects surfaced. (1) The scaffold had been writing `lit_pbr.mat.json` / `lit_pbr.shader.json` as hand-written constants wearing the `bhippi-material@1` / `bhippi-shader@1` markers **while predating the formats** — the first parser to exist would have rejected a new game's own starter material; both now come from the real types plus a real `lit_pbr.wgsl`. (2) The scaffold wrote no `.meta.json` sidecars, so a brand-new project could not produce a Release build: INV-074 blocked it on its own starter content. (3) `bhippi-engine-build::collect_tree` matched scenes on `extension == "bscn"` but the files are `*.bscn.json` (extension `json`), so **no scene had ever been collected** and the structural pass in `collect` had been iterating an empty list since the crate was written. Also worth recording: `EngineSessions::apply_batch` originally validated on a scratch copy then re-lowered against the live document, which is wrong because `spawn` mints a fresh ULID per lowering — a golden test caught it and the single-pass ops are now what commit. | Phase 3 (`ENG-130…139`): the `bhippi-hud@1` widget format, its schema registry, the 2D canvas editor, the Details panel and the runtime renderer — the owner's explicit "a HUD file with all the options, editable by hand". Note `ui/` currently fails `tsc` with 27 errors from an **unfinished plugins feature** another agent left mid-edit (`Sidebar.tsx`, `App.tsx`, `Chat.tsx`, `api.ts` wrappers calling `commands.listPlugins` etc., whose `commands.rs` functions were never registered in `lib.rs`); none touch `ui/src/engine/`. That needs finishing or reverting before the UI builds again. Run `cargo test --workspace` (420 green, 53 suites) + `cargo clippy --workspace` (8 remaining warnings, all in that same unregistered plugin block) + `cargo fmt --check`. |
| 2026-09-01 | claude | **Engine Phase 1 — the AI ↔ engine bridge (`docs/13-…` ENG-110…119).** `bhippi-engine::action` grew from 11 to 23 scene-scoped verbs — `translate`, `look_at`, `set_component_property` (dotted path), `set_tags`, `set_visible`, `set_locked`, `set_mesh`, `set_material`, `attach_script`, `group_entities`, `align_entities`, `distribute_entities` — plus `Op::SetTags` and a `Visibility` component in the schema registry; geometry stays in Rust (look-at quaternion by Shepperd's method with the straight-up degenerate case handled, group centroid, distribute-by-current-order). New `EngineActionBatch::lower` makes a batch **one** transaction / one journal row / one undo step, all-or-nothing, and `session::apply_batch` returns a typed `EngineActionOutcome` per action carrying the failing index, the engine's hint, and that component's `schema::excerpt` (new, alongside `schema::component_names`). New `crates/bhippi-app/src/engine/bridge.rs` scans `<engine_action>` / `<engine_batch>` / `<engine_query>` out of the **live delta stream** (tags split across chunks reassembled, protocol text stripped from the visible answer, unterminated calls dropped rather than half-applied) and `chat.rs` runs a bounded 3-round read→act→verify loop: queries are answered mid-turn, rejected batches come back with their schema for one repair pass. New `engine/query_bridge.rs` answers 12 query kinds (`scene`, `selection`, `entity`, `components`, `children`, `parent`, `find`, `physics`, `assets`, `schema`, `templates`, `weather`, capped at 40 rows) out of the SEC 7.4 `SceneQueries` API that had been built and left unused since ADR-0027. ENG-116: `bhippi_core::EnginePermissionMode` (ask / auto / autonomous) + `engine_permission_mode` / `set_engine_permission_mode` IPC + an Engine-toolbar picker + a plan card gated through the existing `ChatPermissionRequested` channel, defaulting to auto-except-deletions. `engine_context` now carries the user's live selection, the dirty flag and the last six journal rows with actors. `prompts/chat-engine.md` v5 rewritten around the protocol with an explicit "what you cannot do yet" list so the model says so instead of hand-writing a scene file. New IPC: `engine_apply_batch`. | A golden transcript caught a bug reasoning had missed: `apply_batch` first validated on a scratch copy and then re-lowered against the live document, but `spawn` mints a fresh ULID on every lowering — so the second pass created entities that the batch's own later actions, resolved against the first pass, no longer referred to. The ops from the single validating pass are what must commit. ENG-114 (native tool calling) is blocked by architecture, not effort: `CompletionRequest` has `json_schema` but no `tools`, `Delta` has no tool-call variant, and the CLI adapters are subprocesses running their own tool loops — it needs an ADR extending the provider contract first, which is why the tag protocol was made a real in-turn loop rather than left as a stopgap. Note `cargo clippy --all-targets` fails on pre-existing `expect()` in `bhippi-engine-build`'s tests; the documented gate is lib targets. | Phase 2 (`ENG-120…128`): `bhippi-material@1` + `bhippi-shader@1` formats with validation, asset writes on the transaction path with a file-deleting inverse, licence sidecars feeding the INV-074 Release gate, the GLB/OBJ/FBX import pipeline, seeded procedural helpers, provenance tracking, and the blocking content gates — that is also what unblocks the asset-creating verbs deferred out of ENG-110. Run `cargo test --workspace` (366 green, 53 suites) + `cargo clippy --workspace` + `cargo fmt --check` + `tsc --noEmit` + `npm run build` before finishing. |
| 2026-09-01 | claude | **Engine Phase 0 — one write path, one truth (`docs/13-…` ENG-100…109; also closes the long-open ENG-020).** `crates/bhippi-app/src/engine.rs` split into `engine/{mod,session}.rs`; new `EngineSessions` holds one `SceneDocument` per open scene with a single `UndoStack`, dirty flag, revision counter, selection, live `transaction::Session` and a length+mtime disk stamp, behind a process-wide `OnceLock<Mutex<…>>` that **both** the IPC commands and `chat.rs` use — so an `<engine_action>` from a model and a gizmo drag now mutate the same document instead of two copies of one file (INV-070). 16 commands added (`engine_open_scene`/`reload`/`close`/`save`/`save_all`/`undo`/`redo`/`begin`+`record`+`commit`+`cancel_interaction`/`set_selection`/`history`/`weather_presets`/`templates`/`play_world`), `engine_apply_action` regrown to take an actor label and return `EngineEditResult` (state + txn id + actor + touched + journal revision). `bhippi-db`: migration `0011_engine_journal_detail.sql` (scene_rel_path, inverse_json, touched_json, op_count + per-scene index) and a new `EngineRepo` (`upsert_project`, `append` with in-transaction revision allocation, `list`, `latest_revision`, `actor_counts`) — `engine_journal` had existed unused since 2026-08-29, so INV-071 is enforced for the first time. `bhippi-engine` gained `weather.rs` (the eight presets, previously TS-only, plus a test binding them to the `WeatherVolume` schema enum), `compose.rs` (deterministic Main+level+HUD play composition with blake3-derived stable ULIDs), `Op::SetSettings` with stale-write rejection, `EngineAction::{SetWeather, SetSceneSettings}`, an `empty` spawn template, and `UndoStack::peek_undo/peek_redo` for undo labels. UI: `EngineSceneDocument.ts` reduced from 480 lines of logic to types + one `decodeSceneDocument`; `EngineView.tsx` rewritten as a controlled view that dispatches actions and renders `EngineSceneState`; viewport now takes weather presets as data; Add menu and weather picker render the engine's own registries. Bindings regenerated. | The blocker for "the AI controls the engine" was never the model's vocabulary — it was that the *editor* did not use the transaction path, so every agent write raced the webview's React buffer and `EngineSceneChanged` → `reload()` silently discarded unsaved work. Two of the deleted TS functions were also wrong rather than merely misplaced: `newEntityId`/`mergeScenes` produced ids like `level_01JD…` that `SceneDocument::parse` rejects, so a "composed" play scene could never round-trip — which is why composition had to move to Rust in this slice rather than waiting for Phase 6. `serde_json::Value` has no honest specta representation (it exports an externally-tagged union that does not match the bytes actually sent, and the existing `engine_query_*` bindings carry that lie), so `EngineSceneState` ships the scene as its own canonical `bhippi-scene@1` text and the webview decodes once per push. `SceneSettings::default()` and its `#[serde(default = "default_ambient")]` disagree (`[0,0,0]` vs `[0.02,0.02,0.03]`) — left alone, but it will bite whoever assumes an in-memory scene matches a parsed one. | Phase 1 (`ENG-110…119`): grow `EngineAction` to the full ~40 verbs, add `EngineActionBatch` so an AI batch is one transaction/one journal row/one undo, apply mid-turn with a typed result envelope, then `prompts/chat-engine.md` v5. Still open in Phase 0: ENG-104 (autosave + journal replay on crash — the inverse is already stored, nothing reads it), the patching half of ENG-107 (the viewport still rebuilds every entity per push; INV-079's 50 ms budget is unmeasured), and ENG-108's Diff view. Run `cargo test --workspace` + `cargo clippy --workspace` + `cargo fmt --check` + `tsc --noEmit` + `npm run build` before finishing, and tick boxes in `docs/13-…md` only against its Acceptance lines. |
| 2026-09-01 | claude | **Engine AI-control + Unreal-UX plan authored (`docs/13-ENGINE-AI-CONTROL-AND-UNREAL-UX-PLAN.md`), no code changes.** Audited the chat↔engine seam end to end (`chat.rs` `engine_context`/`extract_engine_action_tags`/apply loop, `bhippi-engine` document/transaction/action/schema/api, `bhippi-app::engine` 17 commands, `ui/src/engine/*`, `bhippi-engine-viewport`) and recorded nine verified findings: F1 two write paths (UI `api.writeFile` vs `EngineTransaction`, breaking INV-070 and forking undo), F2 `engine_journal`/`engine_projects` never written (INV-071 unenforced, 0 grep hits), F3 business logic in TypeScript (`EngineSceneDocument.ts` scene creation/weather/duplicate/ULID/merge — INV-073; `mergeScenes` emits non-ULID ids that fail `SceneDocument::parse`), F4 AI vocabulary is 9 single-entity actions scraped from finished prose with no result envelope, F5 play mode is a 56-line RAF placeholder, F6 HUD has no document format (`UiDocument{layout:"health"}` magic string), F7 no `.mat.json`/`.shader.json` schema or parser, F8 viewport renders primitives not real GLB/PBR (`grep -c albedo` → 0), F9 hierarchy not transform-accumulated. Plan is 9 phases / tickets `ENG-100…199` with per-group acceptance lines: P0 one write path + `EngineSessions` + journal + strip TS logic, P1 AI bridge (≈40 verbs, `EngineActionBatch` = one txn = one undo, in-turn apply + result envelope, native tool calling, permission preview, event stream, `chat-engine.md` v5), P2 content generation (`bhippi-material@1`, `bhippi-shader@1`, prefabs, import pipeline, seeded procedural helpers, provenance, blocking gates), P3 HUD system (`bhippi-hud@1` + 12-widget registry + canvas editor + Details + runtime renderer), P4 Unreal UX (docking, Outliner folders, schema-driven Details, Content Browser, viewport toolbar, marquee/vertex snap/Alt-drag, command palette, output log, keymap), P5 rendering truth (GLTF, PBR maps, lighting, transform hierarchy, 55 fps gate, and the forced ENG-168 Bevy-vs-Three decision), P6 real play mode (Rust composition, snapshot/restore, physics, character controller, camera possession, input map, Rhai runtime, HUD runtime, level travel), P7 AI autonomy loop (verify loop, screenshots, playtest, error recovery, Undo AI Change, capability permissions, context budget, locking), P8 hardening. | The `<engine_action>` bridge already works and is journal-less and post-hoc — the blocker for "AI fully controls the engine" is not the model's vocabulary alone but that the *editor itself* does not use the transaction path, so any AI write races the user's React buffer. Migration `0004_engine.sql` has shipped since 2026-08-29 with both engine tables completely unused, which is why there is no AI action history to undo. `protocol.rs` in `bhippi-engine-viewport` is a complete, unused JSON-RPC design against a 13-line Bevy stub — ENG-010 has been "next" in three session logs, so the plan forces an explicit ADR decision instead of leaving Three.js undeclared. | Start Phase 0 (`ENG-100…109`) — it is load-bearing for every later phase; do not start Phase 4 chrome first. Write the ADRs the plan names (HUD format, material format, ENG-168 viewport decision) before the code that depends on them. Run `cargo test --workspace` + `cargo clippy --workspace` + `tsc --noEmit` before finishing, and tick boxes in `docs/13-…md` only against its Acceptance lines. |
| 2026-09-01 | big-pickle | **Engine query API (SEC 7.4 / ADR-0027), engine API + tests only.** Added a pure, deterministic, read-only `bhippi-engine` query surface — `crates/bhippi-engine/src/api.rs` re-exported from the crate: `SceneQueries<'a>` (borrowed facade over `&SceneDocument` + `Option<&AssetIndex>`; `Expansion::{compact, deep}` switched via `compact()`/`deep()`) implementing all 13 SEC 7.4 queries — `get` (scene view + deep hierarchy/settings), `get_entity`/`get_components`/`get_children`/`get_parent` (identity + deep payloads), `find_entities(EntityQuery)` (name/tag/has_component/parent/roots_only), `get_scripts` (`ScriptRef`), `get_asset_users` (entities referencing an asset), `get_asset_dependencies` (other assets co-referenced by the same entities — the scene graph is the dependency source since the scan records no file internals), `get_material_graph` (users + co-shipped textures), `get_shader`, `get_animation_graph` (`AnimationPlayer` clip + `MeshRenderer`/`SkinnedMeshRenderer` mesh), `get_physics` (RigidBody/Collider/CharacterController projection mirroring ADR-0026). All DTOs derive `Serialize/Deserialize/specta::Type` and omit deep fields when `None`; deterministic (authoring order, `BTreeMap` order). Tests: 8 new engine tests (determinism across modes, compact-vs-deep, find/filter, children/parent/missing, asset users/deps, material/shader/animation graphs, physics projection, serde field-omission) — **51 engine tests, full workspace green (0 failed), clippy --workspace clean (0 warnings), architecture test 2/2** (no new crate edges; engine deps unchanged). Also added `specta::Type` derive to `SceneSettings`/`SceneKind` in `document.rs`. Plan SEC 7.4 checkboxes `[x]` + ADR note; PROGRESS status line updated. Scope per owner: API + tests only — no IPC, no World Brain wiring, no UI. | The queries are pure projections over already-present in-memory state, so SEC 7.4 needed no migration and no new crate/deps — unlike 7.1–7.3 there was nothing to persist, only a uniform deterministic surface to expose. `AssetId` lives in `bhippi_types`, not re-exported by `bhippi-engine::asset`, so it must be imported from `bhippi-types` (a private-import compile error). `specta::Type` was missing on `SceneKind`/`SceneSettings`, the only schema types a view exposes. For `Option`-returning query methods, `if x.is_none() { return None }` trips clippy in favour of `x?`. Test-code `unwrap`/`expect` is fine (project rule: "no unwrap outside tests"; the documented gate is `cargo clippy --workspace` lib targets + `cargo test --workspace` — `clippy --all-targets` lints tests and the pre-existing engine test files already trip it). | Expose SEC 7.4 as a follow-up when desired: wire the highest-value queries (get, get_entity, find_entities, get_children/parent, get_asset_users, get_physics) into `bhippi-app` IPC + regenerated bindings + a Brain-panel/inspector section, or move to SEC 7.5 / SEC 9.2. Run `cargo test --workspace` (workspace green incl. 51 engine) + `cargo clippy --workspace` before finishing. |
| 2026-09-01 | big-pickle | **World Brain physics-graph persistence (SEC 7.3 / ADR-0026).** Derived the physics body/collider projection from the per-entity components already snapshotted by SEC 7.1 (`RigidBody` kind/mass/lock_rotation, `Collider` shape/sensor, `CharacterController`) and persisted it into `bhippi-db`: migration `0010_brain_physics.sql` (`brain_physics_bodies` keyed by the entity's stable ULID, FK to `brain_entities` ON DELETE CASCADE, per-scene), a new `bhippi_db::PhysicsBodyRecord` export, `BrainRepo` physics methods (`replace_scene_physics` per-scene replace-after-entities, `physics_bodies_by_project`, `physics_bodies_by_scene`, `physics_body_by_entity`), `WorldBrain::index_scene_document` now derives + persists the scene's physics rows in a second transaction, `WorldBrain` queries `project_physics` / `scene_physics` / `physics_by_entity`, plus IPC commands (`world_brain_physics`, `world_brain_physics_by_scene`, `world_brain_physics_by_entity`) with a `PhysicsBodyView` DTO, regenerated `ipc.ts`, a Brain-panel "Physics" section (expandable body/collider detail) with `.brain-physics*` CSS, and a memory test `world_brain_indexes_physics_bodies_and_colliders`. Collision layers, the collision matrix, joints/constraints and navigation (SEC 7.3 items 3-6) are marked **blocked** on the physics backend (Avian, ENG-053, P5) because the engine has no such data model yet. | The RigidBody/Collider/CharacterController data already lived inside `brain_entities.component_json` from 7.1, so SEC 7.3 bodies+olliders is another mirror of the same 7.1/7.2 pattern — but items 3-6 (layers, matrix, joints, navigation) have no engine source until the physics backend lands, so they were honestly marked blocked rather than invented. The physics row FK targets `brain_entities`, so `replace_scene_physics` must run after `replace_scene_entities` for the same scene (a second txn, mirroring the existing scene upsert → entity replace sequence). Also fixed a pre-existing `cargo fmt` divergence in `bhippi-app/src/brain.rs`. | Start SEC 7.4 engine query API (`scene.get_entity(id)`, `scene.find_entities(query)`, `scene.get_components(entity_id)`, `scene.get_asset_dependencies(asset_id)`, ...) or SEC 9.2; run `cargo test --workspace` (299 green) + `cargo clippy --workspace` + `cargo fmt --check` + `tsc --noEmit`/`npm run build` before finishing. |
| 2026-09-01 | big-pickle | **Project Brain backend + UI + World Brain scene-graph persistence.** Added `bhippi-memory` project-brain IPC to the shell (`crates/bhippi-app::brain`: `project_brain_status`, `rebuild_project_brain`, `list_project_module_cards`, `get_project_module_card`, `search_project_symbols`) plus a `ProjectBrainPanel.tsx` (status stats, rebuild, symbol search, module cards) and `.brain-*` CSS, regenerated bindings. Then landed SEC 7.1 / ADR-0024: `bhippi-memory → bhippi-engine` allowed edge (arch test updated, wrong ADR-0022 comment retargeted to 0024), migration `0008_world_brain.sql` (`brain_scenes` + `brain_entities` with stable ULID PKeys, `parent_id` self-FK, deterministic JSON component payloads, `UNIQUE(project_id, rel_path)`), `BrainRepo` world methods (`scene_by_path/id`, `list_scenes`, `upsert_scene`, `replace_scene_entities`, `scene_entities`, `entity_by_id`, `remove_scene`), and `bhippi-memory::WorldBrain` (`index_scene_document`, `project_scenes`, `scene_entities`, `scene_paths` stable `scene:/a/b#ULID` addresses, `find_entity`, `entity_by_id`, `remove_scene`). Plan SEC 7.1 first four index items checked + note added. | The engine keeps scenes in-memory only, so the World Brain is a peel-off mirror, re-persisting the same `hierarchy()` projection the Hierarchy panel renders; entity rows keep the engine's stable ULID so the AI can address world elements across sessions. `scene_id` on `brain_scenes` (TEXT PK, nullable in sqlx) vs `brain_entities.scene_id` (NOT NULL FK, plain String) trip different `query!` decodes — handle `Option` vs `String` separately. Breadth-first ordering of a per-scene entity replace must insert parents before children so the `parent_id` FK never rejects a child. | Wire World Brain into IPC (`bhippi-app` commands + Brain-panel UI) or start SEC 7.2 asset graph / SEC 9.2; run `cargo test --workspace` (287 green) + `cargo clippy --workspace` before finishing. |
| 2026-09-01 | big-pickle | **Project Brain + World Brain IPC/UI wiring, then World Brain asset-graph persistence (SEC 7.2 / ADR-0025).** Existing engine `AssetIndex` (`AssetRecord`, `AssetKind`, `LicenseState`, `used_by_scenes` cross-ref) rebuilt+persisted into `bhippi-db`: migration `0009_brain_assets.sql` (`brain_assets` keyed by stable AssetId ULID, `kind`/`hash`/`license`/`size_bytes`/`used_by_scenes_json`, `UNIQUE(project_id, rel_path)`), `BrainRepo` asset methods (`asset_by_path/id`, `assets_by_project`, `assets_by_kind`, `replace_project_assets` replace-all-in-one-tx), new `bhippi_db::AssetRecord` export, `WorldBrain` asset methods (`index_asset_index`, `project_assets`, `asset_by_id/path`, `assets_by_kind`, `asset_reverse_usage` resolving scene ids→names), engine `AssetKind::Skeleton` added to cover the 7.2 skeletons item, IPC commands (`world_brain_assets`, `world_brain_assets_by_kind`, `world_brain_asset_usage`, `world_brain_index_assets`) + `AssetView` DTO + registration + regenerated `ipc.ts`, and a Brain-panel asset section (re-index button, per-asset reverse usage) with `.brain-assets*` CSS. Also wired the 7.1 World Brain shell (scene list, expand/collapse, find-entity, index-scene) completed by the prior session. | The engine already had the whole 7.2 data model in-memory (asset kinds incl. materials/shaders/textures/meshes/animations/audio + `used_by_scenes`); the missing piece was persistence + reverse-usage resolution, so SEC 7.2 is mostly mirroring the 7.1 scene-graph pattern. `bhippi_db` uses `AssetId` from `bhippi_types`, and sqlx `query!` row types are anonymous per call site — a shared "row → record" helper can't name `brain_assets::Row`, so inline construction per method is required (same as `SceneRecord`). Reverse usage is stored as quoted `SceneId`s in JSON so resolving to scene names is a lookup, not a scan. | Start SEC 7.3 physics graph (rigid bodies/colliders/layers/joints) which will need the same mirror pattern, or the SEC 7.4 engine query API; run `cargo test --workspace` (298 green) + `cargo clippy --workspace` + `tsc --noEmit`/`npm run build` before finishing. |
| 2026-08-30 | Codex | Added ADR-0023 multi-session Agent workspace, organizer layouts, bounded accessible panel resizing, Single/Multi rail control, project pinning, and cross-panel turn event isolation. | Multiple mounted Chat surfaces require filtering permission and phase events by owned turn id, not only filtering rendered deltas. | Visually QA two chats, mixed chat/CLI, four panels, organizer layouts, pin/reorder, and narrow-window stacking in the native desktop. |
|---|---|---|---|---|
| 2026-08-31 | Codex | Added drag-one-tab-onto-another Multi activation, horizontal wheel/trackpad panning with snap points, keyboard tab movement, and a flat 240 px project sidebar; amended ADR-0023. | The old tab drag only reordered tabs, so it could not express the user's compare intent; the project rail also repeated glow, gradient, borders, and visible controls at every hierarchy level. | Live-check the merge hint, drop transition, horizontal rail movement, and sidebar in Preview when Agent Mode is available. |
| 2026-08-31 | Codex | Matched Smart fit runtime to its organizer thumbnail: the first ordered surface fills the left column while three secondary windows stack on the right, with the same stable rule for three windows and a responsive vertical fallback. | The organizer preview already showed the intended asymmetric layout, but the four-window runtime CSS still declared an equal 2×2 grid. | Live-check four-window Smart fit in Preview when Agent Mode is available. |
| 2026-08-31 | Codex | Stopped timestamp refreshes, focus, and typing from moving Multi panels; synchronized provider selection across mounted chats; retained independent transcripts, drafts, models, queues, streams, and turn ownership per conversation. | Ordering must be an explicit workspace preference, while provider is a workspace routing choice and all conversational state remains scoped by conversation id. | Live-check concurrent sends, shared provider switching, stable Smart fit, and draft restoration in Preview when Agent Mode is available. |
| 2026-08-30 | grok | **Advanced composer menu.** Focus, Agent mode, Predictive text, and Text size were storage-only. Focus now hides chrome; Agent mode auto-approves tools; Predictive text offers Tab completions; Text size actually changes the composer. | `--composer-font-size` was set but CSS used a hardcoded 14px. Agent mode was not wired to permission auto-approve. | Keep Focus as an html class so the Options popover can still turn it off. |
| 2026-08-30 | grok | **Localhost chooser + chat queue actions.** A localhost URL no longer auto-opens; it asks Bhippi browser vs Chrome. While a turn is running, Enter/queue button enqueues the next message (Enter used to Stop). Queue cards can fork, edit, copy, or delete. | Enter-while-streaming called `stop()`, so follow-ups never queued. Localhost auto-open skipped the user's choice of Chrome vs in-app. | Keep the chooser; do not auto-navigate localhost. Queue on Enter when the composer has text. |
| 2026-08-30 | grok | **New session + in-app browser.** New session dimmed the whole app and sat on top of Chat/CLI, so the buttons did nothing. Google never loaded in the pane because child webviews need Tauri `unstable`, and the fallback used an iframe plus dead `<a target=_blank>` links. | `.menu-scrim` at z-index 998 buried `.session-create-menu` at 200. Extra webviews on the main window require `tauri` `unstable`. Google refuses iframes; host it in a native webview. | Keep the session menu `position: fixed` above the scrim. Do not iframe remote sites. |
| 2026-08-30 | grok | **Update click + Computer Use overlay/stop.** Fault-card Update was a silent no-op when the picker id was empty and Settings had no Update button on installed CLIs. Overlay JS looked for `window.__TAURI__` (Tauri 2 only sets internals), so the corner aura never hooked live cursor events. Esc/Esc only armed if the overlay window showed. Codex `-m` was appended after the prompt. | Tauri 2 overlay host is `__TAURI_INTERNALS__` / `isTauri`. Codex `exec [OPTIONS] [PROMPT]` treats tokens after the prompt as prompt text. Emergency stop must arm before overlay chrome. | Keep Computer Use flags after the subcommand and before `-p`/`{prompt}`. Do not put `-m` after a Codex prompt. |
| 2026-08-30 | Codex | Fixed the fault-card Update action end to end, provider subprocess ownership, overlay pointer payloads, animated desktop HUD stop hint, and global Esc/Esc cancellation; removed 240 dead-parent Grok dashboard probes left by the old usage probe. | Tokio timeout drops do not kill `Command::output()` children unless `kill_on_drop(true)` is set; the old image-wide `taskkill` also killed unrelated Codex/Claude/Grok sessions. A successful update looked inert because the stale fault card was never retried. | Keep all provider processes on the shared owned-command builder; live-check the overlay on a Computer Use turn after rebuilding the desktop binary. |
| 2026-08-30 | grok | **Grok chat connect + usage probe.** Headless recipe now uses streaming-json, `--no-leader`, `--always-approve`. Native `~/.grok/bin/grok.exe` wins over npm `.cmd`. Usage probe no longer runs `--max-turns 0` (invalid) or `grok dashboard` (TUI that held grok.exe). Weekly remaining stays **Not reported** because Grok CLI does not print a number. | Grok 1.0.13: `--max-turns` is `1..=u32`. `/usage` headless is a model turn, not a meter. `dashboard` is a TUI. User MCP (npx remotion) can sit silent past the idle timeout. | Do not invent Grok weekly %. If xAI adds a status command, probe that. |
| 2026-08-30 | grok | **Chat providers + Engine snap/dup.** Periodic full detect spawned CLI `--version`/`models` every ~12s and rebuilt the runtime, so chat could not connect. Tick is now local-server ports only; unchanged fingerprints skip rebuild. Engine pane: grid snap 10/1/0.1, World/Local gizmos, Ctrl+D duplicate, Delete, Ctrl+Z/Y undo. AI `Duplicate` now copies components instead of spawning an empty entity. | The 10s loop comment said “400 ms port probes”; it actually ran full `detect()`, including 20s model lists. Auto-enable never stamped `enabled` onto the new rows, so a just-started Ollama stayed unusable until the next scan. | ENG-010 Bevy viewport. ENG-020 shared journal. Vertex snap + Alt-drag. Do not put CLI `--version` on a timer. |
| 2026-08-30 | grok | **Workbench + Engine pipeline (ADR-0022, `prompt.md`).** Browser uses a native Tauri webview so real sites load. Workbench CSS 900px cap removed (expand ~88%, chat floor 320px). Effort slider restyled like the Cursor Faster/Smarter rail. Grok weekly probed via `grok -p /usage` then dashboard; meter shows **% left**. Engine no longer injects a fake demo into every folder — empty grid for non-games; New Game writes Main+HUD+level_01+weather+materials; Play on Main composes HUD+first level; Replace Object imports from disk and keeps transform. | The ~50% panel stop was `max-width: 900px` on `.workspace-workbench`, not the JS clamp. Iframes cannot host google.com; a child Webview can. Grok has no dedicated status quota command — `/usage` is the truthful seam. | Follow `prompt.md` Remaining. ENG-010 Bevy viewport is next. Live-check Grok weekly with `account_live` when signed in. |
| 2026-08-30 | grok | **Vendor account + weekly usage (ADR-0021).** Codex probes the native `codex.exe` over `app-server --stdio` (npm `.cmd` was swallowing JSON-RPC, so weekly never updated). Claude now reads identity from `auth status --json` and live 5h/weekly windows from `claude -p /usage --max-turns 0` with no model spend. Composer meter no longer labels an unknown plan as PRO; refresh overlays the probe unless a newer in-turn snapshot exists. Grok/OpenCode still report account scope only — they do not expose a weekly number. | Codex's npm shim can answer `initialize` and then go silent; weekly data only arrives from the vendor binary. Claude `/usage` is a local slash command (`num_turns: 0`). | Run `cargo test -p bhippi-providers --test account_live -- --ignored --nocapture` when CLIs are signed in; do not invent weekly percentages for Grok/OpenCode. |
| 2026-08-29 | big-pickle | **Engine track P0 (ENG-003): Engine pill + New Game scaffold.** Third workbench pill (`ModeSwitch.tsx` now editor/browser/engine, `WORKBENCH_ORDER`; Ctrl/Cmd+3 jumps straight to Engine, Ctrl+SingleQuote cycles all three), `ui/src/engine/EngineView.tsx` (loading skeleton, empty New-Game form, error fault card, populated game summary), engine styles + 3-track sliding pill in `workbench.css`, Rust side: `crates/bhippi-app::engine` with `get_engine_status` + `engine_create_game_manifest` IPC (sanitized folder name, path-confined, reuses `bhippi-engine::scaffold::write_project`), `bhippi-app -> bhippi-engine` dep + architecture allowed-edge update (17 crates, tests green), `bhippi-engine` re-exports `EngineTrack`, migration `0004_engine.sql` (engine_projects + engine_journal), versioned `prompts/chat-engine.md` v1, bindings regenerated, verified clean (clippy, architecture 2/2, app 1/1 + 2 ignored, tsc + vite build) | The engine crate already carried the whole P0 slice (manifest, scaffold, schema); the seam was the app shell, not the engine. `game_root` resolution (active root, else a single game subfolder) keeps New Game working for both dedicated and mixed projects. A migration file rides along silently via `sqlx::migrate!` | P1 viewport spike (ENG-010..013, the 55 fps gate): build `bhippi-engine-viewport` with `--features bevy` (grid/flycam/cube + embedded child window + JSON-RPC screenshot, else amend ADR-0020); then ENG-020 scene core (open scene, transactions, journal into the 0004 tables) |
| 2026-08-29 | Antigravity | Built **In-Project CLI Sessions & '+' Action Menu** (`crates/bhippi-app::workspace`, `ui/src/screens/CliView.tsx`, `ui/src/styles/cli.css`, `ui/src/chrome/Sidebar.tsx`, `ui/src/App.tsx`): added interactive in-project CLI terminal screen with multi-shell selection (PowerShell, Command Prompt, Git Bash, WSL), real-time command execution in project directory, command history navigation (Up/Down arrows), quick command shortcuts, clear/copy actions, and external terminal window launching; updated sidebar `+` / `New session` action into a dropdown menu with **Chat** and **CLI** options; rendered CLI sessions directly in the sidebar sessions list with `IconTerminal` glyph | Adding a dedicated in-project CLI session engine allows users to create terminal sessions right alongside AI chat sessions in the project workspace, switch between them effortlessly, and pop out into OS terminal windows on demand | Continue testing desktop integration and wire any persistent terminal session logs if desired |
| 2026-08-27 | Codex | Fixed the screenshot-reproduced Computer Use miss: `access my pc and use edge…` now enters the desktop loop; added `/computer <task>` as a deterministic override before the development-discussion veto; added it to autocomplete and `/help`, with palette selection inserting `/computer ` for a task instead of sending an empty turn; updated ADR-0018 | Natural wording can fall between a phrase list and a verb/object heuristic even when both frontend and backend appear broad; the explicit command needs to be checked before every ambiguity veto to be a real override | Keep `/computer` as the deterministic escape hatch and add any newly observed natural phrasing only with a regression test |
| 2026-08-27 | big-pickle | **Computer Use end-to-end proved with a real model**: earlier live runs showed Claude DID return a valid action but the parser only accepted strict JSON, so the relaxed body it actually emits (`{action:mouse_move,x:960,y:540}` — unquoted keys and values) was silently dropped and the test failed. Added a lenient fallback to `computer::parse_action_json`: `quote_relaxed_json` (tokenized quote-around for unquoted keys/string values, numbers/booleans/null passed through, quoted strings and escapes copied verbatim) then the existing `action`→`type` rename; unit-test helper mirrors `chat::extract_computer_action_tags`. Re-ran the live vendor loop: **claude read the real screenshot, returned a parseable action, and the pointer landed and was verified at (960, 540) — the screen centre.** Renamed the entry in the ADR-0018 row from "done (billing-blocked live proof)" to done with a live check | The only thing standing between "vendor answers" and "cursor moves" was trusting that models emit strict JSON. Claude emits relaxed JSON inside `<computer_action>`; strict-only parsing dropped the action every time. The lenient fallback is also the seam `chat.rs` uses, so the fix applies to real chat turns, not just the test | Future S1 sessions: feature is live-proven; continue capability probing / `chat_turns` persistence. Run `cargo test -p bhippi-app --test computer_loop_live -- --ignored --nocapture` whenever a vendor is healthy to keep the proof current |
| 2026-08-26 | Antigravity | Built **Live Coding Animation Bar, In-Turn Exploration Tree, & Fullscreen Work Details Drawer** (`ui/src/screens/ActivityDock.tsx`, `ui/src/screens/Chat.tsx`, `ui/src/styles/activity.css`, `ui/src/styles/chat.css`): implemented hierarchical in-turn work progress (`Exploring N files ▾` with language badges like ⚛ React / 🦀 Rust / 🎨 CSS, line ranges `#L...`, thinking accordion, and live `Working...` pulse); added live coding status bar above composer with glowing `< / >` shimmer, live syntax token ticker, and `[ ⚡ See the work › ]` trigger; built full-width drop-up work details drawer with step-by-step pre-analysis, reasoning monologue, file mutations, and a 1-click **Fullscreen Mode** (`⤢ Fullscreen` / `⤡ Exit`) for deep inspection | Rich hierarchical visual feedback of file explorations before reasoning combined with a live coding animation bar gives the user immediate real-time clarity into exactly what the agent is analyzing and mutating; full-screen inspection provides a complete IDE timeline | Test live desktop chat turn with in-memory `qwen3.8-27b` model; wire durable project-scoped `chat_turns` persistence into `bhippi-db` |
| 2026-08-26 | Antigravity | Redesigned **Chat UI & Layout to Match Reference Aesthetic** (`ui/src/screens/Chat.tsx`, `ui/src/styles/chat.css`): simplified user message into a rounded dark bubble card with subtle on-hover copy/reuse actions; stripped redundant header noise (`USER`, `ASSISTANT Bionic Review` badges); made assistant prose borderless and clean; refined collapsible thinking trigger to minimalist `Thought for Ns ›` / `Worked for Nm ›` text row with indented drawer; added bottom `0 Files With Changes` `[Review Changes]` status bar above composer; restyled composer with rounded card, `+` context attachment trigger, and circular send button | Distraction-free chat ergonomics rely on borderless markdown prose for assistant output and soft card bubbles for user inputs rather than heavy box borders and uppercase role labels; moving review actions from turn headers into a single workspace review bar at the bottom eliminates visual clutter | Test live desktop chat turn with in-memory `qwen3.8-27b` model; wire durable project-scoped `chat_turns` persistence into `bhippi-db` |
| 2026-08-26 | Antigravity | Built **ChatGPT-Style Collapsible Hidden Thinking / Reasoning Process** (`bhippi-app::chat`, `ui/src/screens/Chat.tsx`, `ui/src/styles/chat.css`): added real-time reasoning delta streaming (`Delta::Thinking`, `ChatThoughtDelta`), elapsed thinking duration timing (`thinking_elapsed_ms`), automated `<think>...</think>` & `Thinking Process:` tag extraction and cleansing, and a collapsed-by-default ChatGPT accordion (`Thought for N seconds ▼` pill with pulsating sparkle while running, expandable frosted-glass monologue drawer) | Reasoning models (DeepSeek-R1, Qwen 2.5/3.8, etc.) stream thoughts either via SSE delta reasoning attributes or literal inline `<think>...</think>` text chunks; real-time dual-path tag intercepting in Rust engine plus UI client extraction ensures thoughts are never leaked into user markdown while still remaining 100% inspectable on click | Test live desktop chat turn with in-memory `qwen3.8-27b` model; wire durable project-scoped `chat_turns` persistence into `bhippi-db` |
| 2026-08-26 | Antigravity | Integrated **Bionic & In-Memory Local LLMs Auto-Detection** (`bhippi-providers::detect`, `bhippi-providers::catalog`, `bhippi-providers::model`, `bhippi-providers::openai_compat`, `bhippi-app::chat`, and UI model picker): added dynamic port probing (`detected_port`), fallback ports (`7432`, `1234`, `53166`, `11434`, `8080`, `8000`, `5000`), in-memory loaded models discovery (`qwen3.8-27b`, `google/gemma-4-26b-a4b-qat`, etc.), thinking/reasoning token streaming (`delta.reasoning_content`), and default port routing | Local LLM runtimes (LM Studio, Bionic, llama-server) frequently listen on ports 1234 or 53166 and expose active in-memory models via `/v1/models` data arrays; storing `detected_port` in `ProviderInfo` allows seamless zero-config OpenAI-compatible chat routing to the exact active port | Test live desktop chat turn with loaded `qwen3.8-27b` model; wire durable project-scoped `chat_turns` persistence into `bhippi-db` |
| 2026-08-26 | Antigravity | Built **Review Changes Diff Viewer** (`crates/bhippi-app::review` + `ReviewChangesModal.tsx` + `ProjectHeader` and Chat turn actions): parses unified git diffs and untracked workspace files, provides side-by-side / unified diff inspector, language-specific file icons, expandable file cards, green/red addition/deletion line highlighting with line numbers, turn filter chips, and full-project diff reviews matching user screenshots | Parsing unified git diff output (`git diff HEAD`, `git diff --cached`, and `git status --porcelain`) handles tracked and untracked project additions seamlessly without external diff tools; line-by-line classification with dual line number gutters produces an authentic IDE diff experience | Test live desktop UI review modal with active repo changes; wire durable project-scoped `chat_turns` persistence into `bhippi-db` |
| 2026-08-26 | Antigravity | Eliminated distracting focus-visible highlight rectangle around composer textarea & inputs via `outline: none !important; box-shadow: none !important;` in `app.css` and `chat.css`; added full native detection & catalog support for **Bionic** (CLI + local server detection in `bhippi-providers::catalog` & `command::search_dirs`, skills scanning in `bhippi-skills`, and UI logo mark in `ProviderLogo.tsx`) | Global `:focus-visible` outline applied across multiline textareas inside custom container boxes causes an uncontained inner rectangle; explicitly resetting input/textarea focus states keeps custom borders clean | Connect live debugger action cards to one-click fix prompts in chat; wire durable project-scoped `chat_turns` into `bhippi-db` |
| 2026-08-26 | Antigravity | Added Queued Messages system in Chat UI matching user specification. Messages sent while an agent is busy automatically append to a FIFO queue with collapsible card view (`Queued Messages [N] Sends after agent finishes working`), Send Now (`->`) action that immediately interrupts/forks active turn to run the queued message, Edit (`✏️`) action, and Delete (`🗑️`) action | Reactive FIFO queuing with `useEffect` dispatch on terminal turn states smoothly chains multi-step queries without dropped input | Connect live debugger action cards to one-click fix prompts in chat; wire durable project-scoped `chat_turns` into `bhippi-db` |
| 2026-08-26 | Antigravity | Made Stop turn instantaneous (`tokio::select!` on `provider.complete()` + `biased;` streaming loop + immediate UI state settle in `Chat.tsx`); added 7 new ultra-premium color themes (`sapphire`, `emerald`, `cyberpunk`, `amethyst`, `crimson`, `glacier`, `titanium`) and 1 Translucent Frosted Glass theme (`frosted-glass` with backdrop blur, glass borders, and cyan accent) | Wrapping provider startup in `tokio::select!` with `cancel.changed()` cancels child CLI processes before connection delays occur; backdrop-filter blur across shells with RGBA surfaces produces authentic desktop glassmorphism | Connect live debugger action cards to one-click fix prompts in chat; wire durable project-scoped `chat_turns` into `bhippi-db` |
| 2026-08-26 | Antigravity | Built Chat Slash Commands (`/clean`, `/compact`, `/debug`, `/skills`, `/help`), Cross-AI Skills Discovery & Tagging (`crates/bhippi-skills`, `@skill` injection, Settings › Skills tab, re-scan), Deterministic Non-AI Debugger (`bhippi-app::debugger`, 0 LLM tokens, 15s timeout, multi-compiler + Git conflict parser), and High-Precision Live Elapsed Timing in Chat UI and ActivityDock | Deterministic compiler checks via `cargo check --message-format=json`, `tsc`, and `python` provide instant feedback with zero token cost; automatic discovery across `~/.claude/`, `~/.codex/`, `~/.gemini/config/`, and `.cursorrules` seamlessly surfaces user directives across all AI tools | Connect live debugger action cards to one-click fix prompts in chat; wire durable project-scoped `chat_turns` into `bhippi-db` |
| 2026-08-26 | Antigravity | ADR-0015: Added Computer Use settings panel and full PC vision control. Restricts Computer Use exclusively to vision-capable providers (Claude Code, Codex CLI, Grok CLI) and blocks OpenCode/local models. Added `ComputerUseConfig` in `bhippi-core`, native screen capture and input automation in `bhippi-app::computer`, 5 typed IPC commands, prompt directive in `prompts/chat-computer-use.md`, and rich Settings › Computer Use tab with live display preview testing | Text-only models fail spatial UI reasoning; gating computer use at the config validator and UI matrix prevents runtime coordinate hallucinations. PowerShell/Win32 screen capture and SendInput execute safely with no unsafe Rust code | Test live session computer vision actions on active Windows desktop; wire durable project-scoped `chat_turns` persistence into `bhippi-db` |
| 2026-08-26 | Claude (Opus 5) | ADR-0014: built the in-app workbench and the activity dock. New `bhippi-app::files` with six commands (`list_workspace_dir`, `read_workspace_file`, `write_workspace_file`, `preview_targets`, `read_project_rules`, `write_project_rules`), two-layer path confinement, and tests for traversal/drive-prefix/control-character escape. UI: `ui/src/workbench/` (lazy file tree, in-house per-line tokenizer, textarea-over-highlight editor, loopback-only browser, sliding-pill mode switch), resizable splitter, `ActivityDock` above the composer that also carries the agent's permission questions, project rules in `.bhippi/rules.md` wired through `prompts/chat-rules.md`, a rewritten one-system icon family with per-filetype glyphs, and a chat motion pass. Project identity moved from the toolbar into the sidebar with a hard 20-character name rule | A CSS ellipsis does not solve a long project name: the element still asks for its full width first, so the string has to be cut in JS. An embedded frame with no origin indicator is a phishing surface, which is why the browser is loopback-only and everything else goes to the system browser. Cross-origin frames will not report navigation, so back/forward has to be pane-local or it lies. Reachability must be probed, never assumed — a port list that guesses is worse than none | Finish native 1240×820 visual/keyboard QA of the workbench, both modes, the splitter, and the dock; then durable `chat_turns` with a project foreign key |
| 2026-08-26 | Codex | Corrected ADR-0012 with ADR-0013: persistent pre-project sidebar, native folder selection, Rust-partitioned project conversations, canonical active-project resolution, and per-request CLI working directories | A project-looking UI was not a boundary: conversations were global and all CLIs used one shared directory. Third-party CLI cwd confinement is not an OS sandbox, so the UI and ADR now state the real boundary | Add durable `chat_turns` with a project foreign key, then evaluate provider-specific/OS sandbox enforcement before advertising protection from absolute-path or symlink escapes |
| 2026-08-26 | Codex | Converted the entry and agent shell to a project-first ADE: Rust-persisted project references, local/create/clone flows, detected editor/File Explorer launch actions, Git init, project header, centred new-session composer that docks after send, and Ultra contribution particles; wrote `maekitcode.md` and ADR-0012 | Tauri had no project contract at all; the provider/model/effort backend from the earlier pass was already complete. The in-app browser was unavailable, so native screenshot QA could not be claimed | Finish native 1240×820 visual/keyboard QA, then add database-backed project-scoped `chat_turns`; extend Settings Appearance/Connections only when their runtime controls are real |
| 2026-08-26 | Claude (Opus 5) | Made the CLI providers actually work and gave the picker real model lists. Root cause of "no provider works": on Windows `resolve_command` preferred npm's `.cmd` shim, which forwards args through `cmd.exe` with `%*` — a raw newline **ends the cmd command line**, so every chat prompt was truncated at its first line and every flag after it was dropped. Preferred `.ps1` (regression-pinned). Also: Codex needed `--skip-git-repo-check`; Codex/OpenCode/Claude now run in JSON transcript mode (`transcript.rs`) so answers arrive without vendor banners **and** carry real token counts into the ledger; `list_models_args` reads the vendor's own catalogue at detection (`opencode models` → 361, `codex debug models` → 6 visible slugs, `grok models` → 2); CLIs now spawn in an isolated `~/.bhippi/workspace` instead of inheriting the app's cwd; typed hints for out-of-credit / signed-out / rate-limited; tracing subscriber finally installed in `run()`; `.vscode/launch.json` + `tasks.json` so F5 builds the UI then the desktop binary; conversation delete (bin in the sidebar, two-click confirm); `last_provider` persistence; Settings › Usage rebuilt with a stacked-area chart, Cost/Tokens + 4 windows, share bars, totals, and a Provider/Day breakdown (ADR-0011) | The `.cmd`-vs-`.ps1` choice is not cosmetic — it silently truncates every multi-line prompt, and the vendor answers something plausible from the fragment, so it reads as a bad model rather than a launcher bug. Vendors disagree about `input_tokens`: Claude Code reports only the cache **miss** and names the cache separately (add it back), Codex's figure already includes cache (do not). Re-ordering a validated categorical palette breaks it — a warm-led variant measured ΔE 1.6 deutan vs 8.4 for the documented order. In the UI, a CSS `text-anchor` beats the SVG attribute and pushes the last axis label off the plot | Grok is the one provider still failing and it is **not** a code bug — xAI returns 402, the account is out of credit. Next: capability probing (BHP-015), router/fallback (BHP-020…023), then `chat_turns` persistence in `bhippi-db` |
| 2026-08-26 | ox-alpha | Completed the per-provider model picker: `ProviderInfo.accepts_custom_model` (from catalogue `model_args`) crosses IPC so CLIs without a known list (codex/opencode/grok/kimi) get a working drop-up with a free-text id field instead of a dead disabled chip; added the promised free-text path from the catalogue doc-comment; new `last_model` config round-trip test guards restore-on-launch | The whole chain was already wired end to end — detection → `chat_options` → ModelPicker → `set_provider_model` → `last_model`; only the empty-list CLI face was missing. Full sweep green: fmt/clippy/tests, byte-fresh bindings, tsc/vite, rebuild after `cargo clean -p bhippi-app`, launch smoke test | Continue S1: capability probing (BHP-015), router/fallback (BHP-020…023), then wire `chat_turns` persistence into `bhippi-db` |
| 2026-08-26 | Claude (Opus 5) | BHP-024: built usage metering end to end — `bhippi-core::UsageStore` (daily per-provider rollups in `~/.bhippi/usage.json`, atomic writes, 90-day retention), `bhippi-providers::pricing` list-price table, `bhippi-app::usage` summary with window-scaled caps, three IPC commands (`get_usage_summary`, `set_provider_token_cap`, `clear_usage`), the status-bar ring + drop-up, Settings › Usage with a hand-drawn 30-day chart; retoned the whole palette from green/cool to amber/warm. ADR-0009 records both, `04-PAGES` amended in the same change | The gauge must report the **active** provider or it contradicts the composer; `limit_tokens` has to scale with the selected window or a monthly view silently becomes a daily ring. Sub-agent delegation was attempted and failed to start on every available provider (Codex and Claude lanes were usage-spent), so the two UI surfaces were built in-lane | Wire `chat_turns` persistence into `bhippi-db`, then fold the usage ledger into it (ADR-0009 flags the split as temporary); after that continue S1 capability probing (BHP-015) |
| 2026-08-26 | ox-alpha | Provider management pass: catalogue-driven detection with versions/toggles persisted in `config.toml` (`providers.enabled`), Install buttons running real recipes (INV-003 argv), silent once-per-24h auto-update of enabled CLIs, chat model drop-up fed by `chat_options`, OpenAI-compatible SSE adapter (BHP-017 first slice), CLI spawn adapter (BHP-016 first slice); ADR-0008 opened the app→providers/db edges after the architecture guard caught them | The architecture test is live and strict — it failed the new edges exactly as designed; `cargo clean -p bhippi-app` is required after `ui/dist` changes or the exe ships stale embedded assets | Continue S1: capability probing (BHP-015), router/fallback (BHP-020…023), then wire `chat_turns` persistence |
| 2026-08-26 | ox-alpha | Closed S0: verified BHP-007, built BHP-008 (Tauri v2 shell + full React UI: chat with streaming/tool-cards/permission-cards per ADR-0006, Research tier tables from Rust, Settings with live provider detection), started S1 slice (Provider trait + Demo + Ollama adapters, BHP-010 done / 018 partial), authored CI (BHP-009); ADR-0006 (chat screen) and ADR-0007 (stable toolchain, MSRV 1.85) written | `specta-typescript` 0.29.x is gone from the index — tauri-specta must be rc.24 + specta rc.24 + specta-typescript 0.0.11 as a set; `cargo run -p bhippi-app --bin export-bindings` regenerates `ui/src/lib/ipc.ts` without opening a window; chat history is in-process only until `chat_turns` persistence lands | Wire `chat_turns` persistence through `bhippi-db` (S1 range), then continue S1 detection/capability probing (BHP-011…015) — the UI event contract in `chat.rs` is the seam to build against |
| 2026-08-26 | Codex | Completed BHP-001…004: workspace guardrails, shared types and exact tier budgets, the three-migration SQLite layer with atomic stage checkpoints, and validated config plus OS-keychain isolation | Only stable 1.98 is installed locally; the repository pins 1.79 for CI, while local verification used the installed stable toolchain | Start S0 · BHP-005: implement the typed event bus and the 20/s coalescer |
| 2026-08-26 | Claude (Opus 5) | Wrote the documentation set: architecture, module contracts, data model, pages, pipelines, invariants, agent guide, build order, this file | The spec leaves five structural questions open (A1–A5) and does not define the operational tables (jobs, incidents, learning stats) — both are now captured | Start S0 · BHP-001: create the Cargo workspace and the 14 crates exactly as `01-ARCHITECTURE §12` lists them |

## Session log

| Date | Agent | What it did | What it learned | Next agent should… |
|------|-------|-------------|-----------------|---------------------|
| 2026-08-30 | big-pickle | **Sidebar cleanup + project-scope chat fix.** Removed the top project badge/dropdown from the rail and its dead `.side-project` / `.project-badge*` / `.project-switch-menu*` CSS (`ui/src/chrome/Sidebar.tsx`, `ui/src/styles/app.css`). "New session" is now a full-width **New project** button whose menu offers **Open a folder** (straight to the Windows native folder picker → `api.addProject` → `chooseProject`), **Create a project**, and **Clone from Git** (the project dialog opened with a new `initialMode` prop; both flows browse with the same native picker); session creation moved entirely to each project card's `+` (Chat / CLI). Chat scoping fixed: `<main>` is keyed by `${screen}:${activeProject.path}` in `ui/src/App.tsx`, so the session view remounts when the selected project changes. `handleNewProject("open")` uses plugin-dialog `open`, already registered (`dialog:allow-open`). Verified live in the rebuilt desktop via UIA: clicking another project's head swaps the header path + empty state ("What should we work on in Trying new game?"), "New chat in &lt;project&gt;" creates and opens a conversation scoped to that project, the dropdown is gone, and the New-project menu renders its three options | The chat not following the selected project was not a state bug — the Chat screen only remounted on screen change because it was keyed by screen alone; keying by project makes the switch correct with no backend change. The badge/switcher chrome duplicated what the card head already does, so removing it touched no Rust | Live-check the "Open a folder" native picker open→cancel (skipped by owner choice — only statically verified) and visually QA the new menu + rail at 1240×820 |
| 2026-08-30 | opencode | Enhanced usage metering with account balance tracking: added `balance_micros` field to `ProviderTally` struct, added `record_balance` method to `UsageLedger`, added `balance_usd` to `ProviderUsage`, updated UI components (`ChatUsageMeter.tsx`, `UsagePanel.tsx`) to display account balances for metered providers | Account balance can be tracked per provider in the usage ledger and displayed in the usage UI | Wire up actual balance queries to providers (OpenRouter API has balance endpoints); test usage meter displays correctly |
| 2026-08-30 | opencode | **Refined the glass theme** (`ui/src/styles/tokens.css`, MODE 3: GLASS): fixed the "everything transparent / reads flat" complaint by giving glass panels a touch more body and one coherent backlit-sheet look instead of separate tinted boxes. Rebuilt `--wash` as a layered warm lamp halo + left-side god-ray + cool-depth base so the wallpaper gradient runs top-to-bottom as one canvas; added a shared edge-sheen recipe (crisp top catch-light, hairline side reflection, faint inner top glow) across sidebar, titlebar/statusbar/project-header, composer-shell, popovers/menus, workbench-pane/editor-split, and engine panels; and added a subtle 3D glass bevel to every button/control chip (`.btn-primary/.btn-ghost/.btn-accent/.tool-btn/.icon-btn/.chip/.side-nav-row`, send/composer-circle-send) — a small top catch-light, a side reflection, and a whisper of vertical body so controls read as raised glass instead of flat stickers | The prior 0.52 glass opacity plus identical flat RGBA surfaces made every panel read as the same flat tint bleeding through the wallpaper; distinct panel body + a shared light-catching edge is what sells "one sheet of backlit glass" rather than "transparent boxes over a pretty background" | Re-run native visual QA on the glass theme at 1240×820 (chat + composer + a popover + the workbench split) and tune the bevel/opacity amplitudes if the owner wants more or less sheen |
| 2026-08-30 | opencode | **Dialed the glass theme back to full transparency** (same `tokens.css` GLASS block): the owner rejected the more-opaque pass — "each thing should be transparent and the bg is full screen." Dropped every glass panel's opacity so the full-window wallpaper shows through uniformly: sidebar 0.72→0.52, composer-shell 0.62→0.42, titlebar/statusbar/project-header 0.8→0.5, popovers 0.84→0.62, workbench-pane/editor-split 0.68→0.45, engine-toolbar 0.7→0.5, side chips 0.5→0.32. Kept the richer layered `--wash`, the edge sheen, and the button bevel, but the emphasis is now back on transparency so every pane reads as glass over one full-screen background | The user wanted the cleaner lit-glass look WITHOUT sacrificing the see-through quality that made it feel like real backlit glass; opacity is a tuning knob, and the direction they wanted is "more transparent," not the 0.52→0.6 tighter body I had added | Re-run visual QA; if any panel is too washed out now, raise only that panel's alpha in small steps rather than the whole system |
| 2026-08-30 | opencode | **Less-rounded corners, less reflection, breathing animated background** (`tokens.css` GLASS block + `app.css`/`chat.css` radii): cut the radius tokens (4/6/8/6 → 3/4/5/4) so the composer-shell drops 16→10px, tightened the hardcoded radii on the user message bubble (20→10), popover (14→8), project-start + project-dialog (14→8); softened every sheen/reflection (sheen-top 0.5→0.32, sheen-side 0.16→0.09, composer top inset 0.55→0.28, send/accent radial highlights ~half, button bevel insets ~40% lower); and made the glass background breathe and emit light rays — a slow 16s scale sway + a repeating-conic warm god-ray overlay drifting over 30s on the wallpaper layer `::after`, both GPU-composited and disabled under prefers-reduced-motion | The size/reflection/light are three separate tuning knobs; the owner wants corners flat, glow faint, but the background alive. Because appearance.ts sets the wallpaper's <background> inline, the motion had to live in a pseudo-element overlay rather than the layer's own background animation | Re-run visual QA; if the rays read too strong/weak or the breathe is too fast/slow, adjust the alpha and keyframe durations in the `#app-wallpaper-layer::after` block |
| 2026-08-30 | big-pickle | **Project-card session rail** (W4 §4.2): Rust `WorkspaceSession`/`SessionKind`/`SessionStatus` + `session_status()` + `workspace_sessions()` engine method + `list_workspace_sessions` command resolve provider *id* (for `ProviderLogo`) from the turn's provider *label* via the registry; sidebar rewritten as one smoked-glass card per project (identity head, "N active" pill, session chips with status dots, hover-revealed two-click delete, "Show all N" overflow, empty-project "+"); active-project card pinned with accent rail; rail refetches coalesced on `chatThinking`/`chatTurnDone`; CLI sessions merge client-side; `list_conversations` use dropped from the rail (still a command) | The turn stores the provider **label**, the logo keys on the catalogue **id** — so the session shape carries both and the command layer bridges them; `AwaitingPermission → Paused` (not running) was the right live-state mapping; deleting needs a surface because Chat.tsx itself has none, so the bin moved onto the chips (hover-revealed, two-click armed) | Rebuild the desktop (`cargo clean -p bhippi-app` → `vite build` → `cargo +stable build -p bhippi-app --bin bhippi-desktop`) and visually QA the rail at 1240×820 — especially a project overflowing 8 sessions, an empty project, and a running/paused/failed chip |
| 2026-08-30 | big-pickle | **Fixed the session menus rendering behind/invisible** (owner: clicking a project card's `+` "shows the open panel behind"). Root cause: in glass (and plan) style modes the sidebar carries `backdrop-filter: blur(...)` (`tokens.css` GLASS block), which makes it a *containing block for `position: fixed` descendants* — combined with `.sidebar { overflow: hidden }` the two fixed session menus (`.session-create-menu` z1100 + `.session-menu-scrim` z1090) were laid out against the sidebar box with viewport coords from `getBoundingClientRect()` and clipped out of view behind the sidebar, so clicking `+` produced no visible menu. Fix in `ui/src/chrome/Sidebar.tsx`: render both the New-project menu and every card `+` menu (scrim + menu) through `createPortal(…, document.body)` so `position: fixed` resolves against the viewport again (restores full-window dim + menu at the anchor point). Matches the codebase convention (account/usage popovers use local `position: absolute`; these menus are deliberately fixed and full-window). Root-level z check: 10000/9999/99999 elements are only present in fullscreen-browser/modal/aura states, so the menu stays above all normal chrome | The trap is invisible in devtools inspection because computed styles are right — it's the *containing block* that changes; `backdrop-filter` behaves like `transform` for fixed positioning and is easy to reintroduce by adding any popover with viewport coords inside a blurred panel | Rebuilt & relaunched. Visually confirm on the running app: `+` on any project card (and the New project button) now shows its menu directly under the trigger with the app dimmed |
| 2026-08-30 | big-pickle | **Bottom chat + collapsed rail.** Multi-session tiles were taller than their panel: the chat ran below the panel edge in the bottom tile (owner: `make sure the chat touches down too`). Added compact-override CSS scoped to `.multi-session-workspace` (`ui/src/styles/multi-workspace.css`): tighter composer-zone padding, 12px-radius flush composer-shell, shorter textarea cap (120px, 14px), tighter thread-inner gaps/padding and review-bar padding, so a 320px tile fits messages + composer + footer and the chat fills the panel. The collapsed 48px rail now mirrors the active project's working chat: `.rail-mini` chips (`ui/src/chrome/Sidebar.tsx`) show each session's provider logo (or terminal/chat glyph) with a live-status dot for running/paused/failed, the active conversation lit with the accent ring, sorted active first then running/paused/idle; clicking opens the session (`onOpenSession`). CSS added next to the sidebar collapse rules in `ui/src/styles/app.css` (`.rail-mini-chip` / `.rail-mini-dot` reusing `--ok/--warn/--error` + `pulse-dot`) | `backdrop-filter` still defines the fixed-menu containing-block trap, but portal + viewport-relative `position:fixed` landed the menu exactly under the + (owner confirmed placement is now correct -- the only remaining complaint was the long single-mode composer overflowing a short panel, which the compact scoped composer solves). Collapsed rail had no session rendering at all before, so the owner's request maps to rendering the active project's chips in the 48px rail | Rebuilt (vite + cargo clean/build + clippy clean) and relaunched this session; ask the owner to visually confirm the second tile now touches the panel bottom with no clipped composer, and that collapsing the sidebar shows the chat's provider logos |
